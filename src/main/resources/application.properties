# ===================================================================
# Nome da Aplicacao
# ===================================================================
spring.application.name=docelivery

# ===================================================================
# --- SQL Server Configuration ---
# ===================================================================
# Removido ponto e vírgula solto e garantido o dialeto correto
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=docelivery;encrypt=true;trustServerCertificate=true
spring.datasource.username=SA
spring.datasource.password=1234
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Dialeto Hibernate para SQL Server (Garante compatibilidade com tipos de dados)


# ===================================================================
# --- JPA / Hibernate ---
# ===================================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ===================================================================
# --- Hikari Connection Pool ---
# ===================================================================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=600000

# ===================================================================
# --- Security & JWT ---
# ===================================================================
logging.level.org.springframework.boot.autoconfigure.security=INFO
spring.security.user.name=admin
spring.security.user.password=admin123

#server.error.include-message=always
# A variável JWT_SECRET deve ter pelo menos 32 caracteres para algoritmos modernos
api.security.token.secret=${JWT_SECRET:EssaEhUmaSenhaSuperSecretaDoCaiqueParaODocelivery2026}

# ===================================================================
# --- Logs de Debug ---
# ===================================================================
logging.level.org.springframework.web=INFO
# Reduzido de DEBUG para INFO para não poluir o console, exceto se houver erro
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ===================================================================
# --- Configurações de Upload de Arquivos ---
# ===================================================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.resolve-lazily=true
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=25MB

# ===================================================================
# --- Diretório de Armazenamento (Solução Definitiva) ---
# ===================================================================
# Garante que o ProdutoService encontre a pasta exata no C:
app.upload.dir=C:/docelivery-storage/
# Mantido para compatibilidade, caso algum outro componente utilize
app.upload.path=C:/docelivery-storage/

# ===================================================================
# --- Mercado Pago Configuration ---
# ===================================================================
# Use estas chaves de teste oficiais do Mercado Pago para o seu projeto rodar agora:
mercadopago.public-key=TEST-8b9bbf59-ca60-467e-a694-ce042151399a
mercadopago.access-token=TEST-784355284881001-062414-75fc6ac3391d6589c2562ef20032a5ba-281769591